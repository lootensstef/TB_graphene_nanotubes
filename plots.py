import matplotlib.pyplot as plt
import simulation #separate file that runs all the calculations and outputs the energy bands




def main():
    #material=material_selector() to select a graphene simulation or nanotube simulation
    #precision=input("how many points per reciprocal line?")
    #energybands=simulate(material, precision)
    
    #various pyplot graphic options
    #plt.plot(energybands)










if __name__ == "__main__":
    main()